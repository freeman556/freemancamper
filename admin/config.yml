backend:
  name: git-gateway
  branch: main
media_folder: "static/uploads"
collections:
  - name: "pages"
    files:
      - name: "home"
        file: "index.html"
        fields:
          - {label: "Title", name: "title", widget: "string"}
